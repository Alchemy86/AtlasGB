# Sound — Pokémon Red/Blue

[← Pokémon Red/Blue](../README.md) · [by address](by-address.md) · [by name](by-name.md) · [the structures](structures.md) · [AtlasGB](../../../README.md)

The sound engine is a little interpreter with eight software channels, and
`wChannelCommandPointers` is its program counter — one pointer per channel into a stream of
commands.

Everything in the game asks for a noise the same way: write an id to `wSoundID`. The rest of
this chapter is the interpreter's state — what each channel is playing, its duty cycle, its
vibrato, its pitch slide, its stereo panning.

This chapter is **not verified beyond the symbol file and the live sweep**. Nothing here has an
invariant, because none of it has a checkable relationship to anything else, and the audio work
in the emulator that verifies this atlas is about the console's sound hardware rather than about
this game's driver.
Treat the descriptions as what the names say.

<!-- atlas:begin (table) — generated by tools/render.py from the atlas data; edit the data, not the table -->

**46 entries** · 45 distinct addresses · **43 with a written description** · 1 repeated slots folded onto slot 1.

| address | bytes | symbol | ev | what it is |
|---|---:|---|:--:|---|
| `$C000` | 1 | <a id="s-wUnusedMusicByte"></a>`wUnusedMusicByte` | RL | Declared and never read. Kept because removing it would move everything below it. |
| `$C001` | 1 | <a id="s-wSoundID"></a>`wSoundID` | RL | Which piece of music or sound effect to start. Writing it is how everything in the game asks for a noise. |
| `$C002` | 1 | <a id="s-wMuteAudioAndPauseMusic"></a>`wMuteAudioAndPauseMusic` | RL | Silences the hardware and freezes the sequencer, used while a menu is open. |
| `$C003` | 1 | <a id="s-wDisableChannelOutputWhenSfxEnds"></a>`wDisableChannelOutputWhenSfxEnds` | RL | Silences a music channel that a sound effect borrowed, rather than letting the note resume. |
| `$C004` | 1 | <a id="s-wStereoPanning"></a>`wStereoPanning` | RL | Which hardware channels reach which speaker. |
| `$C005` | 1 | <a id="s-wSavedVolume"></a>`wSavedVolume` | RL | Master volume saved across a fade. |
| `$C006` | 16 | <a id="s-wChannelCommandPointers"></a>`wChannelCommandPointers` | RL | Where each channel is in its command stream — the sound engine is a little interpreter and this is its program counter. Measured: written together with eight sibling per-channel fields (`wChannelSoundIDs`, `wChannelFlags1`, `wChannelDutyCycles`, `wChannelVibratoExtents`, `wChannelVibratoRates`, `wChannelFrequencyLowBytes`, `wChannelVibratoDelayCounterReloadValues`, `wChannelNoteSpeeds`) by one routine (bank 31 `$5AFE`) — the channel-init that arms a new note. |
| `$C016` | 16 | <a id="s-wChannelReturnAddresses"></a>`wChannelReturnAddresses` | RL | Where a channel's command stream resumes after a subroutine call — the return-address stack for `wChannelCommandPointers`' own program counter. Measured across a real playthrough (walk, a forced battle, menus): written from bank 31 `$530A`, during battle-trigger, intro, walk/menus; 9 distinct values seen, 0-235. |
| `$C026` | 8 | <a id="s-wChannelSoundIDs"></a>`wChannelSoundIDs` | RL | What each of the eight software channels is currently playing. |
| `$C02E` | 8 | <a id="s-wChannelFlags1"></a>`wChannelFlags1` | RL | Per-channel state: whether a note is held, whether vibrato is on, and so forth. *(×2, stride 8)* |
| `$C03E` | 8 | <a id="s-wChannelDutyCycles"></a>`wChannelDutyCycles` | RL | The square channels' duty setting per software channel. |
| `$C046` | 8 | <a id="s-wChannelDutyCyclePatterns"></a>`wChannelDutyCyclePatterns` | RL | Which of the four duty-cycle waveforms each square channel is currently using. Measured across a real playthrough (walk, a forced battle, menus): changed value, during intro; values seen: [0]. |
| `$C04E` | 8 | <a id="s-wChannelVibratoDelayCounters"></a>`wChannelVibratoDelayCounters` | RL | Frames left before vibrato starts on each channel, counting down from `wChannelVibratoDelayCounterReloadValues`. Measured across a real playthrough (walk, a forced battle, menus): written from 3 places, chiefly bank 31 `$51F8`, during battle, battle-trigger, intro, post-battle menus, walk/menus; 10 distinct values seen, 0-9. |
| `$C056` | 8 | <a id="s-wChannelVibratoExtents"></a>`wChannelVibratoExtents` | RL | How far each channel's vibrato swings the pitch. Measured across a real playthrough (walk, a forced battle, menus): changed value, during battle-trigger, intro; values seen: [0, 33]. |
| `$C05E` | 8 | <a id="s-wChannelVibratoRates"></a>`wChannelVibratoRates` | RL | How fast each channel's vibrato oscillates. |
| `$C066` | 8 | <a id="s-wChannelFrequencyLowBytes"></a>`wChannelFrequencyLowBytes` | RL | The low byte of each channel's current pitch. |
| `$C06E` | 8 | <a id="s-wChannelVibratoDelayCounterReloadValues"></a>`wChannelVibratoDelayCounterReloadValues` | RL | The value `wChannelVibratoDelayCounters` is reloaded with once its countdown reaches zero. Measured across a real playthrough (walk, a forced battle, menus): changed value, during battle-trigger, intro; values seen: [0, 6, 9]. |
| `$C076` | 8 | <a id="s-wChannelPitchSlideLengthModifiers"></a>`wChannelPitchSlideLengthModifiers` | RL | How many steps a channel's pitch slide takes to complete. Measured across a real playthrough (walk, a forced battle, menus): changed value, during intro; values seen: [0]. |
| `$C07E` | 8 | <a id="s-wChannelPitchSlideFrequencySteps"></a>`wChannelPitchSlideFrequencySteps` | RL | How far the pitch moves each step of a channel's slide. Measured across a real playthrough (walk, a forced battle, menus): changed value, during intro; values seen: [0]. |
| `$C086` | 8 | <a id="s-wChannelPitchSlideFrequencyStepsFractionalPart"></a>`wChannelPitchSlideFrequencyStepsFractionalPart` | RL | The sub-integer remainder of `wChannelPitchSlideFrequencySteps`, carried so a slow slide still lands exactly on its target frequency. Measured across a real playthrough (walk, a forced battle, menus): changed value, during intro; values seen: [0]. |
| `$C08E` | 8 | <a id="s-wChannelPitchSlideCurrentFrequencyFractionalPart"></a>`wChannelPitchSlideCurrentFrequencyFractionalPart` | RL | The sub-integer remainder of a channel's pitch as its slide moves it, kept beside the whole-number frequency bytes. Measured across a real playthrough (walk, a forced battle, menus): changed value, during intro; values seen: [0]. |
| `$C096` | 8 | <a id="s-wChannelPitchSlideCurrentFrequencyHighBytes"></a>`wChannelPitchSlideCurrentFrequencyHighBytes` | RL | The high byte of each channel's pitch as a slide is moving it toward the target frequency. Measured across a real playthrough (walk, a forced battle, menus): changed value, during intro; values seen: [0]. |
| `$C09E` | 8 | <a id="s-wChannelPitchSlideCurrentFrequencyLowBytes"></a>`wChannelPitchSlideCurrentFrequencyLowBytes` | RL |  |
| `$C0A6` | 8 | <a id="s-wChannelPitchSlideTargetFrequencyHighBytes"></a>`wChannelPitchSlideTargetFrequencyHighBytes` | RL | The high byte of the frequency a channel's pitch slide is moving toward. Measured across a real playthrough (walk, a forced battle, menus): changed value, during intro; values seen: [0]. |
| `$C0AE` | 8 | <a id="s-wChannelPitchSlideTargetFrequencyLowBytes"></a>`wChannelPitchSlideTargetFrequencyLowBytes` | RL | The low byte of the frequency a channel's pitch slide is moving toward. Measured across a real playthrough (walk, a forced battle, menus): changed value, during intro; values seen: [0]. |
| `$C0B6` | 8 | <a id="s-wChannelNoteDelayCounters"></a>`wChannelNoteDelayCounters` | RL |  |
| `$C0BE` | 8 | <a id="s-wChannelLoopCounters"></a>`wChannelLoopCounters` | RL | How many repeats are left of a looped section of a channel's command stream. Measured across a real playthrough (walk, a forced battle, menus): written from bank 2 `$5A8A`, during intro; values seen: [0, 1]. |
| `$C0C6` | 8 | <a id="s-wChannelNoteSpeeds"></a>`wChannelNoteSpeeds` | RL | How many frames each of a channel's notes lasts. Measured across a real playthrough (walk, a forced battle, menus): written from bank 31 `$5368` and bank 2 `$5A8A`, during battle-trigger, intro, walk/menus; values seen: [0, 1, 8, 12]. |
| `$C0CE` | 8 | <a id="s-wChannelNoteDelayCountersFractionalPart"></a>`wChannelNoteDelayCountersFractionalPart` | RL | The sub-frame remainder of a channel's note-length countdown (`wChannelNoteDelayCounters`), carried so a note's length stays accurate over many notes. Measured across a real playthrough (walk, a forced battle, menus): written from bank 31 `$55C3` and bank 8 `$5CD0`, during battle, battle-trigger, intro, post-battle menus, walk/menus; 12 distinct values seen, 0-240. |
| `$C0D6` | 8 | <a id="s-wChannelOctaves"></a>`wChannelOctaves` | RL | Each channel's current octave. Measured across a real playthrough (walk, a forced battle, menus): written from bank 31 `$54E2` and bank 8 `$5BEF`, during battle, battle-trigger, intro, post-battle menus, walk/menus; values seen: [0, 4, 5, 6]. |
| `$C0DE` | 8 | <a id="s-wChannelVolumes"></a>`wChannelVolumes` | RL | Each channel's current volume. Measured across a real playthrough (walk, a forced battle, menus): written from bank 31 `$5393` and bank 8 `$5AA0`, during battle, battle-trigger, intro, post-battle menus, walk/menus; 15 distinct values seen, 0-231. |
| `$C0E6` | 1 | <a id="s-wMusicWaveInstrument"></a>`wMusicWaveInstrument` | RL | Which of the wave channel's four instrument samples the current music track is using. Measured across a real playthrough (walk, a forced battle, menus): changed value, during battle-trigger, intro; values seen: [0, 1]. |
| `$C0E7` | 1 | <a id="s-wSfxWaveInstrument"></a>`wSfxWaveInstrument` | RL | Which of the wave channel's four instrument samples the current sound effect is using — kept separate from `wMusicWaveInstrument` so a sound effect borrowing the wave channel does not change what the music resumes with. Measured across a real playthrough (walk, a forced battle, menus): changed value, during intro; values seen: [0]. |
| `$C0E8` | 2 | <a id="s-wMusicTempo"></a>`wMusicTempo` | RL | The current music track's tempo. Measured across a real playthrough (walk, a forced battle, menus): written from bank 2 `$5A7D`, during battle-trigger, intro; values seen: [0, 1]. |
| `$C0EA` | 2 | <a id="s-wSfxTempo"></a>`wSfxTempo` | RL | The current sound effect's tempo, kept separate from `wMusicTempo` for the same reason `wSfxWaveInstrument` is. Measured across a real playthrough (walk, a forced battle, menus): written from 3 places, chiefly bank 2 `$5A80`, during battle, intro; values seen: [0, 1]. |
| `$C0EC` | 2 | <a id="s-wSfxHeaderPointer"></a>`wSfxHeaderPointer` | RL | Pointer to the currently-playing sound effect's own header in ROM. Measured across a real playthrough (walk, a forced battle, menus): written from bank 31 `$59CE`, during battle, intro; values seen: [0, 64, 65, 66]. |
| `$C0EE` | 1 | <a id="s-wNewSoundID"></a>`wNewSoundID` | RL | The sound requested this frame, before the engine has acted on it. |
| `$C0EF` | 1 | <a id="s-wAudioROMBank"></a>`wAudioROMBank` | RL | Which bank the currently playing music lives in — the sound data is banked like everything else. |
| `$C0F0` | 1 | <a id="s-wAudioSavedROMBank"></a>`wAudioSavedROMBank` | RL | The bank to return to after the sound engine has finished. |
| `$C0F1` | 1 | <a id="s-wFrequencyModifier"></a>`wFrequencyModifier` | RL | Added to a channel's frequency bytes as a note plays — the vibrato and pitch-slide effects both work by writing here rather than by touching the note's own base frequency. Measured across a real playthrough (walk, a forced battle, menus): changed value, during battle, intro; values seen: [0, 32, 96, 128, 238, 248]. |
| `$C0F2` | 14 | <a id="s-wTempoModifier"></a>`wTempoModifier` | RL | Per-channel adjustment to the base tempo. Measured across a real playthrough (walk, a forced battle, menus): changed value, during battle, intro; values seen: [0, 1, 16, 48, 64, 96, 128]. |
| `$CFC7` | 1 | <a id="s-wAudioFadeOutControl"></a>`wAudioFadeOutControl` | RL | Whether a fade-out is currently in progress. Measured across a real playthrough (walk, a forced battle, menus): written from 3 places, chiefly bank 1 `$1F80`, during battle-trigger, intro, walk/menus; values seen: [0, 10, 186, 189]. |
| `$CFC8` | 1 | <a id="s-wAudioFadeOutCounterReloadValue"></a>`wAudioFadeOutCounterReloadValue` | RL | The starting value `wAudioFadeOutCounter` counts down from for the current fade. Measured across a real playthrough (walk, a wild battle, a trainer battle, a forced faint, menus, a save): written from bank 17 `$241B`, during intro; values seen: [0, 10]. |
| `$CFC9` | 1 | <a id="s-wAudioFadeOutCounter"></a>`wAudioFadeOutCounter` | RL | Frames left in the current audio fade-out. Measured across a real playthrough (walk, a wild battle, a trainer battle, a forced faint, menus, a save): written from bank 1 `$28E3` and bank 1 `$28EA`, during bag-setup, intro, walk/menus, wild-battle; 11 distinct values, 0-10. |
| `$CFCA` | 1 | <a id="s-wLastMusicSoundID"></a>`wLastMusicSoundID` | RL | What was playing before a sound effect took over, so it can be restored. |

<!-- atlas:end (table) -->
