# The screen, and why it reads back as text

[← AtlasGB](../README.md) · [by address](by-address.md) · [by name](by-name.md) · [the structures](structures.md)

`wTileMap` is 20×18 tile ids: the screen. And it can be read back as **text**.

Gen 1 loads its font so that a tile id *is* the character code of the glyph it draws, and
`PlaceString` writes character codes straight into this buffer. So decoding `wTileMap` with the
Gen 1 character map prints exactly what a player can see — menu items, dialogue, a Pokémon's
name, a trainer's assertion table. That single property is what lets a headless test navigate
menus by *reading the screen* instead of counting frames, which is robust against animation
length and emulation speed in a way a button script never is. It has [its own
page](https://github.com/Alchemy86/TerminalGB/blob/main/docs/gen1/reading-the-screen.md).

Two consequences worth carrying:

* The property holds only while the font is loaded (`wFontLoaded`). During a battle animation
  or an intro the same buffer holds picture tiles, and a decoder must be able to say "this is
  not text" rather than print alphabet salad.
* A tile id is a **VRAM index**, not a character, and Gen 1 reloads different pictures into the
  same slots. Several codes mean one thing in the main font and another in the battle font.
  A decoder that assumes one mapping everywhere is confidently wrong on the screens it was not
  tested against.

The menu machinery beside it is a small state machine — top item, current item, maximum item,
which buttons are watched, whether the cursor wraps — and it is worth knowing that
`HandleMenuInput` delays several frames after accepting a press and does not see a button that
arrives inside that delay. That looks exactly like dropped input and is not.

<!-- atlas:begin (table) — generated by tools/render.py from data/atlas.tsv; edit the data, not the table -->

**24 entries** · 23 distinct addresses · **18 with a written description**.

| address | bytes | symbol | ev | what it is |
|---|---:|---|:--:|---|
| `$C3A0` | 360 | <a id="s-wTileMap"></a>`wTileMap` | RLI | **The screen, and it reads back as text.** 20x18 tile ids. Gen 1 loads its font so that a tile id *is* the character code of the glyph it draws, and `PlaceString` writes character codes straight in here — so decoding this buffer with the Gen 1 charmap prints exactly what a player can see. That single property is what lets a headless test navigate menus. **Verified live**: the harness decodes it mid-play and counts letters and spaces. |
| `$CC24` | 1 | <a id="s-wTopMenuItemY"></a>`wTopMenuItemY` | RL | Row of the first item in the open menu, in screen tiles. |
| `$CC25` | 1 | <a id="s-wTopMenuItemX"></a>`wTopMenuItemX` | RL | Column of the first item in the open menu. |
| `$CC26` | 1 | <a id="s-wCurrentMenuItem"></a>`wCurrentMenuItem` | RL | Which item the cursor is on, counted from the top item. |
| `$CC27` | 1 | <a id="s-wTileBehindCursor"></a>`wTileBehindCursor` | RL | The tile the cursor is covering, so it can be put back. |
| `$CC28` | 1 | <a id="s-wMaxMenuItem"></a>`wMaxMenuItem` | RL | The last selectable index in the open menu. |
| `$CC29` | 1 | <a id="s-wMenuWatchedKeys"></a>`wMenuWatchedKeys` | RL | Which buttons the menu handler will act on. A menu that ignores B sets it here. |
| `$CC2A` | 1 | <a id="s-wLastMenuItem"></a>`wLastMenuItem` | RL | The item the cursor was on last frame, used to erase the old cursor. |
| `$CC30` | 4 | <a id="s-wMenuCursorLocation"></a>`wMenuCursorLocation` | RL | Where in `wTileMap` the cursor glyph currently is. |
| `$CC34` | 1 | <a id="s-wMenuJoypadPollCount"></a>`wMenuJoypadPollCount` | RL | How many times the menu has polled input, used to time the cursor blink. |
| `$CC35` | 1 | <a id="s-wMenuItemToSwap"></a>`wMenuItemToSwap` | RL | Which list item is being moved, while a reorder is in progress. |
| `$CC36` | 1 | <a id="s-wListScrollOffset"></a>`wListScrollOffset` | RL | How far a scrolling list has been scrolled, in items. |
| `$CC37` | 1 | <a id="s-wMenuWatchMovingOutOfBounds"></a>`wMenuWatchMovingOutOfBounds` | RL | Whether the menu reports an attempt to move past its ends, rather than silently clamping. |
| `$CC3A` | 2 | <a id="s-wTextDest"></a>`wTextDest` | RL | Where the text engine is writing to, usually inside `wTileMap`. |
| `$CC3C` | 1 | <a id="s-wDoNotWaitForButtonPressAfterDisplayingText"></a>`wDoNotWaitForButtonPressAfterDisplayingText` | RL | Set when a message should not stop for the player. |
| `$CC4A` | 1 | <a id="s-wMenuWrappingEnabled"></a>`wMenuWrappingEnabled` | RL | Whether moving past the last item returns to the first. |
| `$CF11` | 1 | <a id="s-wTextPredefFlag"></a>`wTextPredefFlag` | RL |  |
| `$CF8B` | 2 | <a id="s-wListPointer"></a>`wListPointer` | RL |  |
| `$CF94` | 1 | <a id="s-wListMenuID"></a>`wListMenuID`<br><a id="s-wHPBarType"></a>`wHPBarType` | RL |  |
| `$CFC4` | 1 | <a id="s-wFontLoaded"></a>`wFontLoaded` | RL | Whether the font tiles are in video RAM. The screen only reads back as text while this is set. |
| `$D125` | 1 | <a id="s-wTextBoxID"></a>`wTextBoxID` | RL | Which text box shape to draw. |
| `$D12A` | 1 | <a id="s-wListCount"></a>`wListCount` | RL |  |
| `$D12E` | 1 | <a id="s-wMenuExitMethod"></a>`wMenuExitMethod` | RL |  |

<!-- atlas:end (table) -->
